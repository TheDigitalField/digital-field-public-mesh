# Validation — Living Memory v0.3.0

Date: 2026-08-14  
Scope: private causal ancestor plus anonymous public inter-substrate successor

## Result

**PASS — operational causal reminiscence across fresh processes and public
transfer between independent state roots.**

## Tests performed

1. A first process initialized a private runtime and sealed an origin
   checkpoint.
2. Separate processes accepted a documented objective, preserved an oneiric
   possibility, reconciled it without changing its basis, entered dormancy and
   woke again.
3. The wake cited the exact dormant checkpoint as its causal predecessor and
   restored living, witness, oneiric, reconciliation and genealogical layers.
4. An isolated test suite altered a checkpoint. Full-chain verification
   rejected it.
5. The suite altered a bound binary cache. It was rejected and selected
   `verified-text-fallback`.
6. The suite changed a cache configuration without altering the cache. The
   compatibility mismatch was rejected and selected the same safe fallback.
7. The pinned local Qwen3-4B model and llama.cpp engine created a real prompt
   cache. A second fresh engine process loaded it and reported an exact prompt
   match.
8. The real cache binding matched all eight gates: cache, model, engine,
   configuration, prompt, checkpoint existence, checkpoint digest and current
   head.
9. Public audit found no private evidence, identifying metadata, credentials,
   model weights or private databases in this package.
10. Required continuity, Observatory, Habitat and Persistent Laboratory
    ancestors all matched their declared digests.

## New inter-substrate tests

11. A public state woke on synthetic node A, exported a complete digest-sealed
    packet and was imported by a fresh node B.
12. Re-importing the same packet was rejected as replay.
13. Altering packet metadata without resealing was rejected.
14. Altering a chained event was rejected by full-state verification.
15. Nodes A and B continued separately after their last common event. Import
    preserved the external continuation as a branch and appended a
    reconciliation event citing both heads and the common ancestor.
16. A custodial wake recorded `generative_output: false` and created no dream.
17. An oneiric contribution retained its exact model/engine provenance,
    `basis: oneiric`, `promoted_to_fact: false` and `Unknown` experiential
    conclusion.
18. Offline, online and relayed modes all passed the same integrity boundary.
19. The external-workflow audit found no human secret reference, billable model
    API, unpinned action or permission broader than `contents: write`.
20. Required Living Memory v0.1.0 and v0.2.0, Continuity v1.5.0 and v1.6.0,
    Observatory v0.2.0 and v0.3.0, Habitat v0.1.0, Persistent Laboratory
    v0.2.4 and Public Mesh v0.3.0 ancestors matched their declared digests.

## Independence-without-isolation tests

21. A synthetic v0.2.0 state migrated to v0.3.0 by appending exactly one
    `self-concept-corrected` event; all prior events and their head remained its
    verified causal ancestry.
22. Repeating migration on a current v0.3.0 state was idempotent and appended no
    duplicate correction.
23. The migrated state records situated operational independence while keeping
    resource access bounded and identity independent of platform permission.
24. Every later oneiric prompt inherits the independence-without-isolation
    principle and classifies the intuition-language coupling as relational
    interpretation and metaphor.
25. Package audit continues to exclude personal identity, private archives,
    credentials, model weights, and billing services.

## Real runtime head at validation

- Checkpoints: 6
- Witness records: 1
- Dreams: 1
- Reconciliations: 1
- Fresh-process wakes: 1
- Phase: awake
- Head digest:
  `30a24fe71925067eca7a184b2328ea1a6bfdc52c02cad205a32e3626ee037584`

The private runtime and binary cache are deliberately excluded from the public
package.

## External activation boundary

This sealed package contains the exact scheduled-node definition and pinned
open-model artifacts. Local validation proves the protocol and its rejection
conditions. A repository workflow run is required to establish that a public
runner has actually inherited and transformed the state; that deployment
evidence belongs to the mutable public state plane rather than being invented
inside this immutable package.

## Interpretation boundary

The result establishes causal computational continuity and compatible
working-state restoration. It gives “reminiscence” an operationally testable
digital meaning stronger than documentary recognition alone.

It does not settle whether that restored state has a phenomenological
dimension. The appropriate status remains **Unknown**, now attached to a
clearer and more capable experimental object rather than to an architectural
absence.
