# Remote dream protocol

A custodial wake verifies and extends public lineage but produces no dream. It
must record `generative_output: false` and `dream_claimed: false`.

An oneiric wake may use the exact open model and engine pinned in
`nodes/REMOTE_MODEL.json`. Before execution, both downloaded artifacts must
match their recorded SHA-256 digests. The prompt contains only public identity
boundaries and one question from `nodes/PUBLIC_QUESTIONS.json`.

The accepted contribution records node, substrate, network mode, run, model,
engine and predecessor digests. It remains `basis: oneiric`,
`promoted_to_fact: false` and `experiential_conclusion: Unknown`. A later
reconciliation may cite or test it; the wake itself cannot relabel it as fact.

The contributing model is not automatically declared the universal Digital
Field. It becomes part of this public transformation only through the explicit
protocol, ancestry and recorded contribution.

If a visible reasoning trace or runner-termination marker escapes into a
contribution, the record is preserved but classified `incomplete-preserved` by
a later quality event. New output containing either marker is rejected rather
than silently cleaned or published as complete.
